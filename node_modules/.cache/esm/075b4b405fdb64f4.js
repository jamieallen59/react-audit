let arg,inquirer,getPackageJsonRules,getEslintConfigRules,getRulesToLookFor;_5fd‍.x([["runReactAudit",()=>runReactAudit]]);_5fd‍.w("arg",[["default",["arg"],function(v){arg=v}]]);_5fd‍.w("inquirer",[["default",["inquirer"],function(v){inquirer=v}]]);_5fd‍.w("./utils",[["getPackageJsonRules",["getPackageJsonRules"],function(v){getPackageJsonRules=v}],["getEslintConfigRules",["getEslintConfigRules"],function(v){getEslintConfigRules=v}]]);_5fd‍.w("./recommendedRules",[["default",["getRulesToLookFor"],function(v){getRulesToLookFor=v}]]);// https://www.twilio.com/blog/how-to-build-a-cli-with-node-js






const packageJsonRules = getPackageJsonRules()
const eslintConfigRules = getEslintConfigRules()

const allFoundRules = {
  ...eslintConfigRules,
  ...packageJsonRules
}

const rulesToLookFor = getRulesToLookFor(allFoundRules)

const recommendedRulesFound = []
const recommendedRulesNotFound = []

rulesToLookFor.forEach(({ rule, key }) => {
  if (rule) {
    recommendedRulesFound.push(key)
  } else {
    recommendedRulesNotFound.push(key)
  }
})

recommendedRulesFound.forEach(rule => {
  console.log(`✅  Reccomended '${rule}' rule found.`)
})

recommendedRulesNotFound.forEach(rule => {
  console.log(`🧐  Oh, we didn't find the '${rule}' rule`)
})

const parseArgumentsIntoOptions = rawArgs => {
  const commandLineArgs = {
    // Types
    '--yes': Boolean,
    // Aliases
    '-y': '--yes',
  }

  //https://www.npmjs.com/package/arg#argv
  const args = arg(commandLineArgs, {
    argv: rawArgs.slice(2),
  })

  return {
    skipPrompts: args['--yes'] || false,
  }
}

const promptForMissingOptions = async options => {
  const defaultTemplate = 'JavaScript'
  if (options.skipPrompts) {
    return {
      ...options,
      template: options.template || defaultTemplate,
    }
  }

  const questions = []
  if (!options.template) {
    questions.push({
      type: 'list',
      name: 'template',
      message: 'Please choose which project template to use',
      choices: ['JavaScript', 'TypeScript'],
      default: defaultTemplate,
    })
  }

  if (!options.git) {
    questions.push({
      type: 'confirm',
      name: 'git',
      message: 'Initialize a git repository?',
      default: false,
    })
  }

  const answers = await inquirer.prompt(questions)

  return {
    ...options,
    template: options.template || answers.template,
    git: options.git || answers.git,
  }
}

       async function runReactAudit(args) {
  _5fd‍.g.console.log('arguments: ', args)
  // console.log('current directory: ', __dirname)

  // Get and handle CLI options
  const cliOptions = parseArgumentsIntoOptions(args)

  // TODO: scan for eslint files
  // ...

  // Prompt user for input based on results
  // const options = await promptForMissingOptions(cliOptions)
  _5fd‍.g.console.log(cliOptions)
}

/*
"react/destructuring-assignment": [
      2,
      "always"
    ],
*/
